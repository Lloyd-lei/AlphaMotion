# ---- Large data sources (download / regenerate via data/prepare_*.py) ----
papers-docs/
data/100style/
data/amass/
data/hy_motion/
data/humanml3d/
data/kimodo/
data/motionx/
data/__pycache__/
data/*.npz
data/*.log

# Whitelist: small committable .npz assets (skin model for mesh viz, ~530 KB)
!notebook/01_resources/assets/*.npz

# ---- Per-experiment caches (rebuild from data) ----
experiment/stage1/runs/*.npz
experiment/stage1/runs/reconstruction/
experiment/stage1/runs/kimodo_export/
experiment/stage1/runs/style_cluster/
experiment/stage1/runs/pair_distance/

# ---- Trained checkpoints (host on Releases / LFS separately if needed) ----
*.pt
*.ckpt
*.bin

# ---- Animations / large media ----
*.gif
*.mp4
*.mov

# Whitelist: notebook experiment GIFs (small, <300 KB each)
!notebook/**/*.gif

# ---- Generic archives ----
*.tar.gz
*.tar.bz2
*.tar.xz
*.zip

# ---- Python ----
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/

# ---- OS / editor ----
.DS_Store
*.swp
*.swo
.idea/
.vscode/
.ipynb_checkpoints/

# ---- Local secrets / config ----
.env
.env.*
*.key
*_secret*
credentials.json

# ---- Other Cursor / Claude session metadata ----
.claude/
.cursor*

# ---- Misc ----
*.log.gz
nohup.out
experiment/kimodo/
